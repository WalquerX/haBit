/target

# Contract build artifacts
contract/target/

# Built WASM files (regenerated by make)
contracts/*.wasm

# Keep the directory
!contracts/.gitkeep

# Docker volumes
docker/bitcoin/data/
docker/mempool/data/

# Docker compose override
docker-compose.override.yml
